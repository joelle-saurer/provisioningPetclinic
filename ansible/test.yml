---
- hosts: localhost #Azure Terraform VM
  gather_facts: True

  tasks:
   
  #run the image
    - name: run container
      docker_container:
          name: containerSelenium
          image: joellesaurer/petclinics:test
          command: run
          ports: 
            - "8086:8080"
          state: present